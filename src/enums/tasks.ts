export enum Tasks {
  query = "Query",
  command = "Command",
}

export enum ArgsTasks {
  query = "-q",
  command = "-c",
}

export enum FileFolders {
  query = "queries",
  command = "commands",
}

export enum FileTypes {
  query = "query",
  command = "command",
}
